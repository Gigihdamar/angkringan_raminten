Folder ini sengaja dikosongkan (hanya sebagai placeholder struktur MVC).

Pada project ini, logic controller digabung langsung ke dalam Blueprint di
folder `routes/` (mis. `routes/admin.py`, `routes/customer.py`) agar lebih
sederhana untuk dipelajari mahasiswa, sementara `models/` tetap dipisah
penuh sesuai konsep OOP yang diwajibkan pada laporan UAS.
